using System;

class Program
{
    static void Main()
    {
        // Print the welcome message
        Console.WriteLine("Welcome to Acme Accounting Systems");

        // Print the additional message with escaped quotes for "accounting"
        Console.WriteLine("Remember, we’re \"accounting\" on you!");

        // Optional: Wait for the user to press Enter before closing the console window
        Console.ReadLine();
    }
}